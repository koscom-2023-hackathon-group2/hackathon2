import { SingleHistoryInfoWrapper } from "../styles/HistoryEmotion";

const SingleHistoryInfo = (props) => {
  return <SingleHistoryInfoWrapper>{props.children}</SingleHistoryInfoWrapper>;
};

export default SingleHistoryInfo;
